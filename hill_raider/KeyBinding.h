#pragma once

namespace HillRaider
{
	// --------------------------------------------------
	// The KeyBinding enum is used to represent actions
	// which have been bound to one or more keys.
	// --------------------------------------------------
	enum class KeyBinding {
		ENTER = 0,
		ESCAPE,
		UP,
		RIGHT,
		DOWN,
		LEFT,
		SPACE,
		E
	};
}